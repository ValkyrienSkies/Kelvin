{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "{$mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}"
  },
  "license": "${mod_license}",
  "icon": "assets/${mod_id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "org.valkyrienskies.kelvin.KelvinModFabric"
      }
    ],
    "client": [
      {
        "adapter": "kotlin",
        "value": "org.valkyrienskies.kelvin.KelvinModFabric${'$'}Client"
      }
    ],
    "preLaunch": [
      {
        "adapter": "kotlin",
        "value": "org.valkyrienskies.kelvin.ValkyrienPreLaunch"
      }
    ]
  },
  "mixins": [
    "kelvin-common.mixins.json",
    "kelvin.mixins.json"
  ],
  "depends": {
    "minecraft": "${minecraft_version}",
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "architectury": ">=${architectury_version}"
  }
}